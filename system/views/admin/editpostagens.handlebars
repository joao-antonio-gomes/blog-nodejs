{{#if erros}}
    <div class="alert alert-danger">
        {{#each erros}}
            • {{texto}} <br/>
        {{/each}}
    </div>
{{/if}}

<h2 class="card-title">Editar postagem:</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/postagens/edit" method="post" class="d-flex flex-column">
            <input type="hidden" name="id" value="{{_id}}">
            <label for="titulo" class="form-label">Titulo</label>
            <input type="text" id="titulo" name="titulo" placeholder="Titulo da postagem" class="form-control" value="{{titulo}}">
            <label for="slug" class="form-label mt-3">Slug</label>
            <input type="text" id="slug" name="slug" placeholder="Slug da postagem" class="form-control" value="{{slug}}">
            <label for="descricao" class="form-label mt-3">Descricao</label>
            <input type="text" id="descricao" name="descricao" placeholder="Descricao da postagem"
                   class="form-control" value="{{descricao}}">
            <label for="conteudo" class="form-label mt-3">Conteudo</label>
            <textarea name="conteudo" id="conteudo" cols="30" rows="5" class="form-control"
                      placeholder="Conteúdo da postagem">{{conteudo}}</textarea>
            <label for="categoria" class="form-label mt-3">Categoria</label>
            <select name="categoria" class="form-select" id="categoria">
                {{#if categorias}}
                    <option value="0"></option>
                {{/if}}
                {{#each categorias}}
                    <option value="{{_id}}">{{nome}}</option>
                {{else}}
                    <option value="0">Nenhuma categoria registrada</option>
                {{/each}}
            </select>

            <button type="submit" class="align-self-end btn btn-primary mt-4">Atualizar postagem</button>
        </form>
    </div>
</div>
