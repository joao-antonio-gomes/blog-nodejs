<div class=" p-5 bg-light border rounded-3">
    <h2>Bem vindo ao blog!</h2>
    <p class="fs-4 fw-light">Este é um blog simples, desenvolvido com Node.js, Express e MongoDB.</p>
    <hr>
    <p>Aqui você pode ler notícias e novidades sobre o mundo da programação.</p>
    <a class="btn btn-primary" type="button">Crie uma conta</a>
</div>
<hr>
<h2>Postagens recentes</h2>

{{#each postagens}}
    <div class="card mt-3">
        <div class="card-body">
            <h5 class="card-title">{{titulo}}</h5>
            <p class="fst-italic">{{descricao}}</p>
            <a href="postagem/{{slug}}" class="btn btn-success" type="button">Leia mais</a>
            <hr>
            <small>Categoria: {{categoria.nome}}</small><br/>
            <small>Data de publicação: {{date}}</small>
        </div>
    </div>
{{else}}
    <h5 class="mt-3">Nenhuma postagem recente!</h5>
{{/each}}
